#include<stdio.h>
#include<conio.h>

int n;
char dt[100];

int min(int x,int y)
{
	if(x>y)return = y;
}
void pass()
{
	if(dt[i] == dt[j]) return pass(i+1,j-1);
	else return min(c(i,j-1)+1,c(c(i+1,j)+1);
}
void main(void)
{
	FILE *fp;
	fp = fopen("palin.in","r");
		fscanf(fp,"%d\n",&n);	
		fgets(dt,100,fp);
	fclose(fp);
}

