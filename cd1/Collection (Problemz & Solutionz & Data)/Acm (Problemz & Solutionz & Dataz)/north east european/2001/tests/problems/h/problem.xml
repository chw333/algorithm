<?xml version='1.0'?>
<!-- THIS FILE WAS AUTOMATICALLY GENERATED BY ContestGen AT Mon Nov 26 17:04:11 GMT+03:00 2001 -->
<problem id='h'>
<testers>
  <acm-tester
    tests        = '18'
    inputs       = 'pairs.{test##}'
    answers      = 'pairs.{test##}a'
    input-names  = 'pairs.in'
    output-names = 'pairs.out'
    time-limit   = '5s'
  >
    <checker
       class     = 'pcms2.testlib.Checker'
       program   = '!pairs.exe'
       command   = '{exec:!/!.!} {input} {output} {answer} result.xml -appes'
       exec-id   = 'bin.x86.exe.win32'
       result    = 'result.xml'
    />
  </acm-tester>
</testers>

<statement>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<title>Pairs of integers</title>
</head>
<body>

<h2>Pairs of integers</h2>

<table>
<tr><td colspan="1" rowspan="1"><strong>Input file</strong></td><td colspan="1" rowspan="1"><tt>pairs.in</tt></td></tr>
<tr><td colspan="1" rowspan="1"><strong>Output file</strong></td><td colspan="1" rowspan="1"><tt>pairs.out</tt></td></tr>
</table>

<p>You are to find all pairs of integers such that their sum is equal to the
given integer number N and the second number results from the
first one by striking out one of its digits. The first integer always has
at least two digits and starts with a non-zero digit. The second integer
always has one digit less than the first integer and may start with a zero
digit.</p>

<h3>Input</h3>

<p>The input file consists of a single integer N (10 &#8804; N &#8804; 10<sup>9</sup>).</p>

<h3>Output</h3>

<p>On the first line of the output file write the total number of different 
pairs of integers that satisfy the problem statement. On the following
lines write all those pairs. Write one pair on a line in ascending order of
the first integer in the pair. Each pair must be written in the following format:</p>

<p><tt>X + Y = N</tt></p>

<p>Here X, Y, and N, must be replaced with the corresponding integer numbers. There should
be exactly one space on both sides of '+' and '=' characters.</p>

<h3>Sample input</h3>

<pre xml:space="preserve"><tt>302</tt></pre>

<h3>Output for the sample input</h3>

<pre xml:space="preserve"><tt>5
251 + 51 = 302
275 + 27 = 302
276 + 26 = 302
281 + 21 = 302
301 + 01 = 302</tt></pre>

</body>
</html>
</statement>
</problem>
